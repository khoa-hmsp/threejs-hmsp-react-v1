import Model from './Classes/Model.js';

export default class OpenBox extends Model {
  constructor() {
    super('openBox');
  }

  update() {
    // this.animation.mixer.update(this.time.delta * 0.001);
  }
}
